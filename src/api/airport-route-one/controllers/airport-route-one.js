'use strict';

/**
 * airport-route-one controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::airport-route-one.airport-route-one');
