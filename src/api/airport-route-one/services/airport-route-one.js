'use strict';

/**
 * airport-route-one service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::airport-route-one.airport-route-one');
